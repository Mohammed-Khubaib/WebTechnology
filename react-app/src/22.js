import React from 'react';
import ReactDOM from 'react-dom/client';

function College() {
  return <h2>CSE Department</h2>;
}

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<College />);
